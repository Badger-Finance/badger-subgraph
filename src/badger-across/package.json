{
  "name": "badger-across",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ darruma/badger-across",
    "create-local": "graph create --node http://localhost:8020/ darruma/badger-across",
    "remove-local": "graph remove --node http://localhost:8020/ darruma/badger-across",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 darruma/badger-across"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
