type Token implements ERC20 @entity {
  id: ID!
  name: String!
  symbol: String!
  decimals: BigInt!
  totalSupply: BigInt!
}

type TokenBalance @entity {
  id: ID!
  balance: BigInt!
  token: Token!
}
